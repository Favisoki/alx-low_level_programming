#include <time.h>
#include <stdio.h>


/**
 * main - Entry point
 *
 * Return: zero
 *
 */

int main(void)
{
	char c;


	for (c = 'a'; c <= 'z'; ++c)
	{
		putchar(c);
		for (C = 'A'; C <= 'Z'; ++C)
		{
			putchar(C);
		}
	}
	putchar('\n');

	return (0);
}
