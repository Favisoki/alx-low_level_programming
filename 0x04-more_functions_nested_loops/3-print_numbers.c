#include "main.h"
#include <stdio.h>

/**
 *mul - multiplies two numbers
 *@a: first number
 *@b: second number
 *
 * Return: 1 if uppercase, 0 if lowercase
 */
void print_numbers(void)
{
	for (int n = 0; n <= 9; n++)
	{
		_putchar(n);
		_putchar('\n');
	}
}

