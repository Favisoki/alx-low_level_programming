#include "main.h"
#include <stdio.h>

/**
 *mul - multiplies two numbers
 *@a: first number
 *@b: second number
 *
 * Return: 1 if uppercase, 0 if lowercase
 */
int mul(int a, int b)
{
	return (a * b);
}

