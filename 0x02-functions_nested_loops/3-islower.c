#include "main.h"
#include <stdio.h>

/**
 * _islower - checks if a character is lowercase
 *@c: character to check
 *
 * Return: 0 for success
 *
*/
int _islower(int c)
{
	int c;

	if (c == islower(c))
	{
		ret = 0;
	}
	else
	{
		ret = 1;
	}
	return (ret);
}
